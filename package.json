{
  "name": "scrapmycaruae-headless",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "astro build && wrangler deploy",
    "cf-typegen": "wrangler types",
    "seo:inventory": "node --env-file=.env scripts/export-seo-inventory.mjs",
    "seo:audit": "node scripts/audit-seo-inventory.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.2",
    "@wix/blog": "^1.0.661",
    "@wix/sdk": "^1.21.16",
    "astro": "^7.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "wrangler": "^4.136.1"
  }
}
