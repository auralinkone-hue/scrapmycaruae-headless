---
import type { VehicleMake, VehicleModel, VehicleYear } from '../lib/vehicle-data';

interface Props {
  makes: VehicleMake[];
  models: VehicleModel[];
  years: VehicleYear[];
  dataError?: string;
}

const {
  makes = [],
  models = [],
  years = [],
  dataError = ''
} = Astro.props;

const featuredMakes = makes.filter((item) => item.featuredMake);
const regularMakes = makes.filter((item) => !item.featuredMake);

const countryCodes = [
  ['🇦🇪','United Arab Emirates','+971'],['🇦🇫','Afghanistan','+93'],['🇦🇱','Albania','+355'],['🇩🇿','Algeria','+213'],
  ['🇦🇸','American Samoa','+1684'],['🇦🇩','Andorra','+376'],['🇦🇴','Angola','+244'],['🇦🇮','Anguilla','+1264'],
  ['🇦🇬','Antigua & Barbuda','+1268'],['🇦🇷','Argentina','+54'],['🇦🇲','Armenia','+374'],['🇦🇼','Aruba','+297'],
  ['🇦🇺','Australia','+61'],['🇦🇹','Austria','+43'],['🇦🇿','Azerbaijan','+994'],['🇧🇸','Bahamas','+1242'],
  ['🇧🇭','Bahrain','+973'],['🇧🇩','Bangladesh','+880'],['🇧🇧','Barbados','+1246'],['🇧🇾','Belarus','+375'],
  ['🇧🇪','Belgium','+32'],['🇧🇿','Belize','+501'],['🇧🇯','Benin','+229'],['🇧🇲','Bermuda','+1441'],
  ['🇧🇹','Bhutan','+975'],['🇧🇴','Bolivia','+591'],['🇧🇦','Bosnia & Herzegovina','+387'],['🇧🇼','Botswana','+267'],
  ['🇧🇷','Brazil','+55'],['🇮🇴','British Indian Ocean Territory','+246'],['🇻🇬','British Virgin Islands','+1284'],['🇧🇳','Brunei','+673'],
  ['🇧🇬','Bulgaria','+359'],['🇧🇫','Burkina Faso','+226'],['🇧🇮','Burundi','+257'],['🇰🇭','Cambodia','+855'],
  ['🇨🇲','Cameroon','+237'],['🇨🇦','Canada','+1'],['🇨🇻','Cape Verde','+238'],['🇰🇾','Cayman Islands','+1345'],
  ['🇨🇫','Central African Republic','+236'],['🇹🇩','Chad','+235'],['🇨🇱','Chile','+56'],['🇨🇳','China','+86'],
  ['🇨🇽','Christmas Island','+61'],['🇨🇨','Cocos Islands','+61'],['🇨🇴','Colombia','+57'],['🇰🇲','Comoros','+269'],
  ['🇨🇬','Congo','+242'],['🇨🇩','Congo (DRC)','+243'],['🇨🇰','Cook Islands','+682'],['🇨🇷','Costa Rica','+506'],
  ['🇭🇷','Croatia','+385'],['🇨🇺','Cuba','+53'],['🇨🇼','Curaçao','+599'],['🇨🇾','Cyprus','+357'],
  ['🇨🇿','Czechia','+420'],['🇩🇰','Denmark','+45'],['🇩🇯','Djibouti','+253'],['🇩🇲','Dominica','+1767'],
  ['🇩🇴','Dominican Republic','+1809'],['🇪🇨','Ecuador','+593'],['🇪🇬','Egypt','+20'],['🇸🇻','El Salvador','+503'],
  ['🇬🇶','Equatorial Guinea','+240'],['🇪🇷','Eritrea','+291'],['🇪🇪','Estonia','+372'],['🇸🇿','Eswatini','+268'],
  ['🇪🇹','Ethiopia','+251'],['🇫🇰','Falkland Islands','+500'],['🇫🇴','Faroe Islands','+298'],['🇫🇯','Fiji','+679'],
  ['🇫🇮','Finland','+358'],['🇫🇷','France','+33'],['🇬🇫','French Guiana','+594'],['🇵🇫','French Polynesia','+689'],
  ['🇬🇦','Gabon','+241'],['🇬🇲','Gambia','+220'],['🇬🇪','Georgia','+995'],['🇩🇪','Germany','+49'],
  ['🇬🇭','Ghana','+233'],['🇬🇮','Gibraltar','+350'],['🇬🇷','Greece','+30'],['🇬🇱','Greenland','+299'],
  ['🇬🇩','Grenada','+1473'],['🇬🇵','Guadeloupe','+590'],['🇬🇺','Guam','+1671'],['🇬🇹','Guatemala','+502'],
  ['🇬🇬','Guernsey','+44'],['🇬🇳','Guinea','+224'],['🇬🇼','Guinea-Bissau','+245'],['🇬🇾','Guyana','+592'],
  ['🇭🇹','Haiti','+509'],['🇭🇳','Honduras','+504'],['🇭🇰','Hong Kong','+852'],['🇭🇺','Hungary','+36'],
  ['🇮🇸','Iceland','+354'],['🇮🇳','India','+91'],['🇮🇩','Indonesia','+62'],['🇮🇷','Iran','+98'],
  ['🇮🇶','Iraq','+964'],['🇮🇪','Ireland','+353'],['🇮🇲','Isle of Man','+44'],['🇮🇱','Israel','+972'],
  ['🇮🇹','Italy','+39'],['🇨🇮',"Côte d’Ivoire",'+225'],['🇯🇲','Jamaica','+1876'],['🇯🇵','Japan','+81'],
  ['🇯🇪','Jersey','+44'],['🇯🇴','Jordan','+962'],['🇰🇿','Kazakhstan','+7'],['🇰🇪','Kenya','+254'],
  ['🇰🇮','Kiribati','+686'],['🇰🇼','Kuwait','+965'],['🇰🇬','Kyrgyzstan','+996'],['🇱🇦','Laos','+856'],
  ['🇱🇻','Latvia','+371'],['🇱🇧','Lebanon','+961'],['🇱🇸','Lesotho','+266'],['🇱🇷','Liberia','+231'],
  ['🇱🇾','Libya','+218'],['🇱🇮','Liechtenstein','+423'],['🇱🇹','Lithuania','+370'],['🇱🇺','Luxembourg','+352'],
  ['🇲🇴','Macao','+853'],['🇲🇬','Madagascar','+261'],['🇲🇼','Malawi','+265'],['🇲🇾','Malaysia','+60'],
  ['🇲🇻','Maldives','+960'],['🇲🇱','Mali','+223'],['🇲🇹','Malta','+356'],['🇲🇭','Marshall Islands','+692'],
  ['🇲🇶','Martinique','+596'],['🇲🇷','Mauritania','+222'],['🇲🇺','Mauritius','+230'],['🇾🇹','Mayotte','+262'],
  ['🇲🇽','Mexico','+52'],['🇫🇲','Micronesia','+691'],['🇲🇩','Moldova','+373'],['🇲🇨','Monaco','+377'],
  ['🇲🇳','Mongolia','+976'],['🇲🇪','Montenegro','+382'],['🇲🇸','Montserrat','+1664'],['🇲🇦','Morocco','+212'],
  ['🇲🇿','Mozambique','+258'],['🇲🇲','Myanmar','+95'],['🇳🇦','Namibia','+264'],['🇳🇷','Nauru','+674'],
  ['🇳🇵','Nepal','+977'],['🇳🇱','Netherlands','+31'],['🇳🇨','New Caledonia','+687'],['🇳🇿','New Zealand','+64'],
  ['🇳🇮','Nicaragua','+505'],['🇳🇪','Niger','+227'],['🇳🇬','Nigeria','+234'],['🇳🇺','Niue','+683'],
  ['🇳🇫','Norfolk Island','+672'],['🇰🇵','North Korea','+850'],['🇲🇰','North Macedonia','+389'],['🇲🇵','Northern Mariana Islands','+1670'],
  ['🇳🇴','Norway','+47'],['🇴🇲','Oman','+968'],['🇵🇰','Pakistan','+92'],['🇵🇼','Palau','+680'],
  ['🇵🇸','Palestine','+970'],['🇵🇦','Panama','+507'],['🇵🇬','Papua New Guinea','+675'],['🇵🇾','Paraguay','+595'],
  ['🇵🇪','Peru','+51'],['🇵🇭','Philippines','+63'],['🇵🇱','Poland','+48'],['🇵🇹','Portugal','+351'],
  ['🇵🇷','Puerto Rico','+1787'],['🇶🇦','Qatar','+974'],['🇷🇪','Réunion','+262'],['🇷🇴','Romania','+40'],
  ['🇷🇺','Russia','+7'],['🇷🇼','Rwanda','+250'],['🇧🇱','Saint Barthélemy','+590'],['🇸🇭','Saint Helena','+290'],
  ['🇰🇳','Saint Kitts & Nevis','+1869'],['🇱🇨','Saint Lucia','+1758'],['🇲🇫','Saint Martin','+590'],['🇵🇲','Saint Pierre & Miquelon','+508'],
  ['🇻🇨','Saint Vincent & Grenadines','+1784'],['🇼🇸','Samoa','+685'],['🇸🇲','San Marino','+378'],['🇸🇹','São Tomé & Príncipe','+239'],
  ['🇸🇦','Saudi Arabia','+966'],['🇸🇳','Senegal','+221'],['🇷🇸','Serbia','+381'],['🇸🇨','Seychelles','+248'],
  ['🇸🇱','Sierra Leone','+232'],['🇸🇬','Singapore','+65'],['🇸🇽','Sint Maarten','+1721'],['🇸🇰','Slovakia','+421'],
  ['🇸🇮','Slovenia','+386'],['🇸🇧','Solomon Islands','+677'],['🇸🇴','Somalia','+252'],['🇿🇦','South Africa','+27'],
  ['🇰🇷','South Korea','+82'],['🇸🇸','South Sudan','+211'],['🇪🇸','Spain','+34'],['🇱🇰','Sri Lanka','+94'],
  ['🇸🇩','Sudan','+249'],['🇸🇷','Suriname','+597'],['🇸🇪','Sweden','+46'],['🇨🇭','Switzerland','+41'],
  ['🇸🇾','Syria','+963'],['🇹🇼','Taiwan','+886'],['🇹🇯','Tajikistan','+992'],['🇹🇿','Tanzania','+255'],
  ['🇹🇭','Thailand','+66'],['🇹🇱','Timor-Leste','+670'],['🇹🇬','Togo','+228'],['🇹🇰','Tokelau','+690'],
  ['🇹🇴','Tonga','+676'],['🇹🇹','Trinidad & Tobago','+1868'],['🇹🇳','Tunisia','+216'],['🇹🇷','Türkiye','+90'],
  ['🇹🇲','Turkmenistan','+993'],['🇹🇨','Turks & Caicos Islands','+1649'],['🇹🇻','Tuvalu','+688'],['🇻🇮','U.S. Virgin Islands','+1340'],
  ['🇺🇬','Uganda','+256'],['🇺🇦','Ukraine','+380'],['🇬🇧','United Kingdom','+44'],['🇺🇸','United States','+1'],
  ['🇺🇾','Uruguay','+598'],['🇺🇿','Uzbekistan','+998'],['🇻🇺','Vanuatu','+678'],['🇻🇦','Vatican City','+39'],
  ['🇻🇪','Venezuela','+58'],['🇻🇳','Vietnam','+84'],['🇼🇫','Wallis & Futuna','+681'],['🇾🇪','Yemen','+967'],
  ['🇿🇲','Zambia','+260'],['🇿🇼','Zimbabwe','+263']
] as const;
---

<div class="quote-card" data-quote-form>
  <div class="quote-head">
    <span class="quote-kicker"><i class="ph ph-car-profile" aria-hidden="true"></i>Free vehicle valuation</span>
    <h2>Tell us about your car</h2>
    <p>Choose your vehicle details. It only takes a moment.</p>
  </div>
 
  <form novalidate>
    <div class="panel active" data-step-panel="1">
      <label>
        <span class="sr-only">Vehicle make</span>
        <span class="field-control">
          <i class="ph ph-steering-wheel" aria-hidden="true"></i>
          <select name="make" data-make required aria-label="Vehicle make" disabled={Boolean(dataError || makes.length === 0)}>
            <option value="">Select make</option>

            {featuredMakes.length > 0 && (
              <optgroup label="Popular makes">
                {featuredMakes.map((item) => (
                  <option value={item.make}>{item.make}</option>
                ))}
              </optgroup>
            )}

            {regularMakes.length > 0 && (
              <optgroup label="All makes">
                {regularMakes.map((item) => (
                  <option value={item.make}>{item.make}</option>
                ))}
              </optgroup>
            )}
          </select>
        </span>
      </label>

      <label>
        <span class="sr-only">Vehicle model</span>
        <span class="field-control">
          <i class="ph ph-car-profile" aria-hidden="true"></i>
          <select name="model" data-model required aria-label="Vehicle model" disabled>
            <option value="">Select make first</option>
          </select>
        </span>
      </label>

      <label>
        <span class="sr-only">Vehicle year</span>
        <span class="field-control">
          <i class="ph ph-calendar-blank" aria-hidden="true"></i>
          <select name="year" required aria-label="Vehicle year" disabled={Boolean(dataError || years.length === 0)}>
            <option value="">Select year</option>
            {years.map((item) => (
              <option value={item.year}>{item.year}</option>
            ))}
          </select>
        </span>
      </label>

      {dataError && (
        <div class="data-error" role="alert">
          Vehicle options could not be loaded from Wix CMS. Check the three collection IDs and public read permissions.
        </div>
      )}

      <button type="button" class="continue" data-next>
        Get My Free Valuation
        <i class="ph ph-arrow-right" aria-hidden="true"></i>
      </button>
    </div>

    <div class="panel" data-step-panel="2">
      <button type="button" class="back" data-back><i class="ph ph-arrow-left" aria-hidden="true"></i>Back</button>

      <div class="vehicle-summary" data-vehicle-summary></div>

      <label>
        <span class="sr-only">Your name</span>
        <span class="field-control">
          <i class="ph ph-user" aria-hidden="true"></i>
          <input
            name="name"
            autocomplete="name"
            placeholder="Enter your name"
            required
          />
        </span>
      </label>

      <label>
        <span class="sr-only">Mobile or WhatsApp number</span>
        <div class="phone-wrap">
          <select name="countryCode" class="country-code" aria-label="Country calling code" required>
            {countryCodes.map(([flag, country, code]) => (
              <option value={code} selected={code === '+971'}>{flag} {code} · {country}</option>
            ))}
          </select>
          <span class="phone-input-control">
            <i class="ph ph-device-mobile" aria-hidden="true"></i>
            <input
              name="whatsapp"
              inputmode="tel"
              autocomplete="tel"
              placeholder="Mobile / WhatsApp number"
              required
            />
          </span>
        </div>
      </label>

      <label class="hp-field" aria-hidden="true">
        <span>Website</span>
        <input name="website" tabindex="-1" autocomplete="off" />
      </label>

      <button type="submit" class="submit" data-submit>
        Get My Free Valuation
        <i class="ph ph-arrow-right" aria-hidden="true"></i>
      </button>

      <div class="submit-message" data-submit-message hidden></div>

      <p class="fine-print">
        No obligation. Your details will only be used to contact you about your vehicle valuation.
      </p>
    </div>
  </form>
</div>

<script define:vars={{ vehicleModels: models }}>
  (() => {
    const roots = document.querySelectorAll('[data-quote-form]');

    roots.forEach((root) => {
      const panels = root.querySelectorAll('[data-step-panel]');
       const form = root.querySelector('form');
      const makeSelect = root.querySelector('[data-make]');
      const modelSelect = root.querySelector('[data-model]');
      const summary = root.querySelector('[data-vehicle-summary]');

      if (!(form instanceof HTMLFormElement)) return;
      if (!(makeSelect instanceof HTMLSelectElement)) return;
      if (!(modelSelect instanceof HTMLSelectElement)) return;

      const setStep = (step) => {
        panels.forEach((panel) => {
          panel.classList.toggle(
            'active',
            panel.getAttribute('data-step-panel') === String(step)
          );
        });
 
      };

      const populateModels = (make) => {
        modelSelect.replaceChildren();

        const placeholder = document.createElement('option');
        placeholder.value = '';
        placeholder.textContent = make ? 'Select model' : 'Select make first';
        modelSelect.appendChild(placeholder);

        if (!make) {
          modelSelect.disabled = true;
          return;
        }

        const matching = vehicleModels.filter((item) => item.make === make);

        matching.forEach((item) => {
          const option = document.createElement('option');
          option.value = item.model;
          option.textContent = item.model;
          modelSelect.appendChild(option);
        });

        modelSelect.disabled = matching.length === 0;

      };

      makeSelect.addEventListener('change', () => {
        populateModels(makeSelect.value);
      });

      root.querySelector('[data-next]')?.addEventListener('click', () => {
        const firstPanel = root.querySelector('[data-step-panel="1"]');
        const required = firstPanel?.querySelectorAll(
          'input[required], select[required]'
        ) || [];

        const invalid = Array.from(required).find(
          (field) =>
            field instanceof HTMLInputElement || field instanceof HTMLSelectElement
              ? !field.checkValidity()
              : false
        );

        if (
          invalid instanceof HTMLInputElement ||
          invalid instanceof HTMLSelectElement
        ) {
          invalid.reportValidity();
          return;
        }

        const data = new FormData(form);

        if (summary) {
          summary.textContent =
            `${data.get('make')} ${data.get('model')} · ${data.get('year')}`;
        }

        setStep(2);
      });

      root.querySelector('[data-back]')?.addEventListener('click', () => {
        setStep(1);
      });

      form.addEventListener('submit', async (event) => {
        event.preventDefault();

        const required = form.querySelectorAll(
          'input[required], select[required]'
        );

        const invalid = Array.from(required).find(
          (field) =>
            field instanceof HTMLInputElement || field instanceof HTMLSelectElement
              ? !field.checkValidity()
              : false
        );

        if (
          invalid instanceof HTMLInputElement ||
          invalid instanceof HTMLSelectElement
        ) {
          invalid.reportValidity();
          return;
        }

        const submitButton = root.querySelector('[data-submit]');
        const submitMessage = root.querySelector('[data-submit-message]');
        const data = Object.fromEntries(new FormData(form).entries());

        if (submitButton instanceof HTMLButtonElement) {
          submitButton.disabled = true;
          submitButton.innerHTML = 'Submitting… <i class="ph ph-circle-notch" aria-hidden="true"></i>';
        }

        if (submitMessage instanceof HTMLElement) {
          submitMessage.hidden = true;
          submitMessage.textContent = '';
          submitMessage.classList.remove('error');
        }

        try {
          const response = await fetch('/api/quote', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify(data)
          });

          const result = await response.json();

          if (!response.ok || !result.ok) {
            throw new Error(result.message || 'Submission failed.');
          }

          window.location.href = result.redirect || '/thank-you';
        } catch (error) {
          if (submitMessage instanceof HTMLElement) {
            submitMessage.hidden = false;
            submitMessage.classList.add('error');
            submitMessage.textContent =
              error instanceof Error
                ? error.message
                : 'We could not submit your request right now.';
          }

          if (submitButton instanceof HTMLButtonElement) {
            submitButton.disabled = false;
            submitButton.innerHTML = 'Get My Free Valuation <i class="ph ph-arrow-right" aria-hidden="true"></i>';
          }
        }
      });
    });
  })();
</script>

<style>
  .quote-card {
    width: 100%;
    padding: 26px;
    border: 1px solid rgba(170, 224, 151, .34);
    border-radius: 22px;
    background: #173d18;
    box-shadow: 0 20px 54px rgba(19, 45, 18, .20);
  }

  .quote-head .quote-kicker {
    color: #a9df94;
    font-size: 12px;
    font-weight: 850;
    letter-spacing: .09em;
    text-transform: uppercase;
    display: inline-flex;
    align-items: center;
    gap: 7px;
  }

  .quote-kicker .ph {
    font-size: 15px;
  }

  .quote-head h2 {
    margin: 6px 0 7px;
    font-size: 29px;
    line-height: 1.08;
    letter-spacing: -.04em;
    color: #fff;
  }

  .quote-head p {
    margin: 0;
    color: #d5e4d2;
    font-size: 14px;
    line-height: 1.55;
  }

  .panel {
    display: none;
    gap: 14px;
    margin-top: 20px;
  }

  .panel.active {
    display: grid;
  }

  label {
    display: grid;
    gap: 7px;
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .field-control,
  .phone-input-control {
    position: relative;
    display: block;
  }

  .field-control > .ph,
  .phone-input-control > .ph {
    position: absolute;
    top: 50%;
    left: 16px;
    z-index: 1;
    color: #39723b;
    font-size: 19px;
    transform: translateY(-50%);
    pointer-events: none;
  }

  input,
  select {
    width: 100%;
    min-height: 50px;
    min-width: 0;
    padding: 0 14px 0 48px;
    border: 1px solid #d1dbce;
    border-radius: 12px;
    outline: none;
    background: #fbfdf9;
    color: #151715;
    font: inherit;
    font-size: 15px;
    transition: border-color 160ms ease, box-shadow 160ms ease, background 160ms ease;
  }

  select:disabled,
  input:disabled {
    background: #f3f5f1;
    color: #959b93;
    cursor: not-allowed;
  }

  input:focus,
  select:focus {
    border-color: #8ccb78;
    box-shadow: 0 0 0 3px rgba(169, 223, 148, .28);
  }

  input:focus-visible,
  select:focus-visible,
  .continue:focus-visible,
  .submit:focus-visible,
  .back:focus-visible {
    outline: 3px solid rgba(47, 111, 45, .16);
    outline-offset: 2px;
  }

  .vehicle-summary {
    padding: 11px 13px;
    border-radius: 10px;
    background: #e1f2dc;
    color: #194c1e;
    font-size: 13px;
    font-weight: 850;
  }

  .phone-wrap {
    display: grid;
    grid-template-columns: 116px 1fr;
    align-items: center;
    border: 1px solid #d1dbce;
    border-radius: 12px;
    overflow: hidden;
    background: #fbfdf9;
  }

  .phone-wrap:focus-within {
    border-color: #8ccb78;
    box-shadow: 0 0 0 4px rgba(169, 223, 148, .28);
  }

  .country-code {
    min-width: 0;
    border: 0;
    border-right: 1px solid #e4e7e1;
    border-radius: 0;
    background: #f8faf7;
    box-shadow: none !important;
    padding: 0 26px 0 10px;
    font-size: 12px;
    cursor: pointer;
  }

  .phone-wrap input {
    width: 100%;
    min-width: 0;
    border: 0;
    border-radius: 0;
    box-shadow: none !important;
  }

  .phone-wrap input::placeholder {
    color: #8d938b;
    font-size: 13px;
  }

  .phone-input-control {
    min-width: 0;
  }

  .phone-input-control > .ph {
    left: 12px;
    font-size: 17px;
  }

  .phone-input-control input {
    padding-left: 40px;
  }

  .continue,
  .submit {
    min-height: 50px;
    margin-top: 3px;
    padding: 0 18px;
    border: 0;
    border-radius: 12px;
    background: #a9df94;
    color: #173d18;
    font: inherit;
    font-weight: 850;
    cursor: pointer;
    transition: transform 160ms ease, background 160ms ease, opacity 160ms ease;
  }

  .continue:hover,
  .submit:hover {
    transform: translateY(-1px);
    background: #c4edb5;
  }

  .continue,
  .submit {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 9px;
  }

  .continue {
    justify-content: center;
  }

  .continue .ph,
  .submit .ph {
    font-size: 18px;
  }

  .submit:disabled .ph-circle-notch {
    animation: smc-spin .8s linear infinite;
  }

  @keyframes smc-spin {
    to { transform: rotate(360deg); }
  }

  .back {
    width: fit-content;
    padding: 0;
    border: 0;
    background: none;
    color: #c7efba;
    font: inherit;
    font-size: 13px;
    font-weight: 800;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }

  .back .ph {
    font-size: 16px;
  }

  .fine-print {
    margin: 0;
    color: #d0dfcc;
    font-size: 11px;
    line-height: 1.5;
  }

  .hp-field {
    position: absolute !important;
    width: 1px !important;
    height: 1px !important;
    overflow: hidden !important;
    clip: rect(0 0 0 0) !important;
    white-space: nowrap !important;
  }

  .submit:disabled {
    opacity: .72;
    cursor: wait;
  }

  .submit-message {
    padding: 11px 12px;
    border-radius: 10px;
    background: #e1f2dc;
    color: #194c1e;
    font-size: 12px;
    line-height: 1.45;
  }

  .submit-message.error {
    background: #fff0f0;
    color: #842727;
  }

  .data-error {
    margin-top: 2px;
    padding: 12px 13px;
    border-radius: 10px;
    background: #fff0f0;
    color: #842727;
    font-size: 12px;
    line-height: 1.45;
  }

  @media (max-width: 620px) {
    .quote-card {
      padding: 18px;
      border-radius: 16px;
      box-shadow: 0 14px 30px rgba(10, 34, 11, .24);
    }

    .quote-head h2 {
      font-size: 25px;
    }

    .quote-head p {
      font-size: 13px;
    }

    .quote-head .quote-kicker {
      font-size: 11px;
    }

    label {
      gap: 6px;
    }

    input,
    select {
      min-height: 48px;
      border-radius: 10px;
      background-color: #fbfdf9;
      font-size: 15px;
    }

    .phone-wrap {
      grid-template-columns: minmax(96px, 30%) minmax(0, 1fr);
    }

    .country-code {
      padding-left: 8px;
      padding-right: 20px;
      font-size: 11px;
    }

    .phone-wrap input {
      padding-right: 9px;
    }

    .phone-wrap input::placeholder {
      font-size: 11px;
    }

    .panel {
      gap: 12px;
      margin-top: 16px;
    }

    .continue,
    .submit {
      min-height: 50px;
      border-radius: 11px;
    }
  }
</style>
